(()=>{const isMobile=matchMedia('(max-width:700px)').matches;if(isMobile)document.querySelectorAll('img[data-mobile-src]').forEach(img=>{img.src=img.dataset.mobileSrc});const images=[...document.querySelectorAll('.page-image')],progress=document.querySelector('.progress'),scanline=document.querySelector('.scanline'),lightbox=document.querySelector('.lightbox'),enlarged=lightbox.querySelector('img');let ticking=false;const update=()=>{const max=document.documentElement.scrollHeight-innerHeight,ratio=max?scrollY/max:0;progress.style.width=`${ratio*100}%`;scanline.style.transform=`translateY(${Math.round(ratio*(innerHeight+20))}px)`;if(isMobile)return;images.forEach(img=>{const rect=img.getBoundingClientRect(),distance=(rect.top+rect.height/2-innerHeight/2)/innerHeight;img.style.setProperty('--drift',`${Math.max(-10,Math.min(10,-distance*5))}px`);if(img.classList.contains('is-visible'))img.style.transform='translate3d(0,var(--drift),0) scale(1)'})};if(!isMobile)addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(()=>{update();ticking=false});ticking=true}},{passive:true});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>entry.isIntersecting&&entry.target.classList.add('is-visible')),{threshold:.03});images.forEach(img=>observer.observe(img));images.forEach(img=>img.addEventListener('click',()=>{enlarged.src=img.currentSrc||img.src;enlarged.alt=img.alt;lightbox.classList.add('open');document.body.style.overflow='hidden'}));const close=()=>{lightbox.classList.remove('open');document.body.style.overflow=''};lightbox.querySelector('.close').addEventListener('click',close);lightbox.addEventListener('click',event=>event.target===lightbox&&close());addEventListener('keydown',event=>event.key==='Escape'&&close());update()})();
